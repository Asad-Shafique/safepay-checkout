name: safepay_checkout
description: A simple and easy-to-integrate Flutter package for Safepay Checkout. Clients can seamlessly process payments by calling a single widget and handling responses through the onPaymentSuccess callback, without managing complex payment logic.
version: 1.0.0
homepage: https://github.com/Asad-Shafique/safepay-checkout

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"


dependencies:
  flutter:
    sdk: flutter
  flutter_inappwebview: ^6.1.5
  http: ^1.2.2
    

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0


flutter:

  assets:
    - assets/
